# 极简记账APP设计文档

## 1. 产品功能说明

### 核心定位
- 只关心“每个月我存了多少/花了多少/结余多少”，以及“我的积蓄随时间如何变化”
- 账本按“月”划分

### 功能模块

#### A. 首页/月视图
- 默认进入当月
- 显示：月份标题、本月期初积蓄、本月收入、本月支出、本月结余、本月期末积蓄
- 支出按类型汇总列表：每个类型的支出金额与占比
- 快速入口：+ 记一笔（收入/支出）
- 月份切换：上月/下月按钮

#### B. 记一笔（交易录入）
- 选择：收入 or 支出
- 输入：金额、日期、类型（支出必选自定义类型）、备注（可选）
- 保存后立即刷新该月与后续月份的结转数据
- 支持编辑/删除交易

#### C. 支出类型管理
- 列表展示所有类型（系统默认 + 用户自定义）
- 新增类型（名称必填）
- 编辑类型
- 删除类型（删除时需要处理历史交易：可要求用户选择“转移到其他类型”或“保留但标记为已删除类型”）

#### D. 数据存储
- 本地存储：IndexedDB
- 支持数据导出（CSV/JSON）

## 2. 数据结构

### Transaction（交易记录）
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | String | 唯一标识符 |
| date | Date | 交易日期（具体到日） |
| amount | Number | 交易金额（正数） |
| type | String | 交易类型：income  expense |
| categoryId | String | 支出类别ID（expense必须有；income可选） |
| note | String | 备注（可选） |

### Category（支出类型）
| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | String | 唯一标识符 |
| name | String | 类型名称 |
| icon | String | 图标（可选） |
| sortOrder | Number | 排序顺序 |
| deletable | Boolean | 是否可删除（系统默认类型不可删） |

### MonthlySummary（月度汇总，可选）
| 字段名 | 类型 | 描述 |
|-------|------|------|
| month | String | 月份标识（如 "2024-01"） |
| openingBalance | Number | 期初积蓄 |
| totalIncome | Number | 本月收入 |
| totalExpense | Number | 本月支出 |
| balance | Number | 本月结余 |
| closingBalance | Number | 期末积蓄 |

## 3. 页面结构

### 首页/月视图
```
┌─────────────────────────────────┐
│           2024年1月             │
│ 〈       月份       〉           │
├─────────────────────────────────┤
│ 期初积蓄：¥10,000.00             │
│ 本月收入：¥5,000.00   💰        │
│ 本月支出：¥3,000.00   💸        │
│ 本月结余：¥2,000.00              │
│ 期末积蓄：¥12,000.00             │
├─────────────────────────────────┤
│ 支出类型汇总                     │
├─────────────────────────────────┤
│ 餐饮    ¥1,000.00   33.3%        │
│ 交通    ¥500.00     16.7%        │
│ 房租    ¥1,500.00   50.0%        │
└─────────────────────────────────┘
┌─────────────────────────────────┐
│           + 记一笔              │
└─────────────────────────────────┘
```

### 记一笔页面
```
┌─────────────────────────────────┐
│         记一笔                  │
├─────────────────────────────────┤
│ 🔄 切换：收入 / 支出            │
├─────────────────────────────────┤
│ 金额：__________                │
│ 日期：2024-01-15                │
│ 类型：[下拉选择]                │
│ 备注：__________                │
├─────────────────────────────────┤
│         保存                    │
└─────────────────────────────────┘
```

### 支出类型管理页面
```
┌─────────────────────────────────┐
│       支出类型管理              │
├─────────────────────────────────┤
│ 餐饮           [编辑] [删除]    │
│ 交通           [编辑] [删除]    │
│ 房租           [编辑] [删除]    │
│ 娱乐           [编辑] [删除]    │
├─────────────────────────────────┤
│         + 新增类型              │
└─────────────────────────────────┘
```

## 4. 月度账务规则
1. 每个月独立显示本月数据：本月收入、本月支出、本月结余、本月期末积蓄
2. "上个月结余"必须在下个月自动结转为"期初积蓄/期初余额（Opening Balance）"
3. 结转金额不能被计入下个月的"收入"，只能作为期初余额
4. 本月结余 = 本月收入 - 本月支出
5. 本月期末积蓄 = 本月期初积蓄 + 本月结余
6. 如果用户修改历史月份的收入/支出，必须自动级联更新后续月份的期初积蓄与期末积蓄（保持全局一致）
7. 允许跨月查看：可左右切换月份/选择月份

## 5. 技术选型
- 前端框架：Vue 3
- 状态管理：Pinia
- 本地存储：IndexedDB（使用 Dexie.js 简化操作）
- UI 组件：原生 HTML + CSS（极简风格）
- 构建工具：Vite

## 6. 数据存储方案
- 使用 IndexedDB 存储交易记录和支出类型
- 月度汇总数据可实时计算，也可缓存
- 支持数据导出为 CSV/JSON 格式